{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run db:migrate && npm run storage:setup && npm run bootstrap:deploy && npm run build",
  "regions": ["fra1"],
  "crons": [
    {
      "path": "/api/jobs/run",
      "schedule": "0 4 * * *"
    }
  ]
}
